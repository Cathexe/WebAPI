How to set up and run the project: 
    - To be able to sign into the CRUD page you need to register an account. 
    - You will need to Search localhost:3000/register

How authentication works: 
    - For authentication, it takes the username from the Users DataBase and checks if the user that was input matches
    - The same for passwords but it just unhashes the hashed passwords. 
